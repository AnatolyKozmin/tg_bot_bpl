"""
Конфигурация для генерации билетов.
Настройте координаты, размеры шрифтов и цвета под ваш дизайн.
"""

# =============================================================================
# ШРИФТЫ
# =============================================================================

import os

# Путь к шрифту (автоопределение: Docker или локально)
if os.path.exists("/app"):
    # На сервере в Docker
    FONT_PATH = "/app/denistina_en (2).ttf"
    FONT_PATH_FALLBACK = "/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf"
else:
    # На локальной машине
    FONT_PATH = "denistina_en (2).ttf"
    FONT_PATH_FALLBACK = "DejaVuSans-Bold.ttf"

# Размеры шрифтов (в пикселях)
FONT_SIZES = {
    'name': 100,         # Фамилия и Имя
    'telegram_id': 35,  # Telegram ID
}

# =============================================================================
# КООРДИНАТЫ ТЕКСТА (x, y)
# Координаты указывают ЦЕНТР текста (text anchor='mm' - middle-middle)
# =============================================================================

TEXT_POSITIONS = {
    'qr_code': (750, 1080),          # Центр QR-кода
    'telegram_id': (600, 1300),      # Telegram ID (под QR)
    'name': (600, 540),             # Фамилия Имя (под telegram_id)
}

# =============================================================================
# ЦВЕТА ТЕКСТА (RGB или название)
# =============================================================================

TEXT_COLORS = {
    'name': 'white',            # Цвет имени
    'telegram_id': '#555555',   # Цвет telegram_id (серый)
}

# =============================================================================
# QR-КОД
# =============================================================================

QR_CONFIG = {
    'size': (400, 400),         # Размер QR-кода (ширина, высота)
    'box_size': 10,             # Размер одного "пикселя" QR (минимум 1)
    'border': 2,                # Толщина белой рамки вокруг QR
}

# =============================================================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# =============================================================================

# QR-код содержит только Telegram ID пользователя
QR_DATA_FORMAT = "{telegram_id}"

# Формат имени файла билета
TICKET_FILENAME_FORMAT = "ticket_{telegram_id}.png"

